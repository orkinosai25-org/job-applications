# job-applications

This repository now contains an editable markdown companion for the CV/application
update draft at `YÖK+FORMATLI+CVIsmail.md`.

The originally requested binary source file `YÖK+FORMATLI+CVIsmail.docx` was not present in
the repository or its `main` branch at the time of update, so the markdown file is prepared
as a transfer-ready source for the final formatted CV.